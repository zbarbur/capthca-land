# PROJECT PERSISTENCE: CAPTHCA Duality 🐍🌓

This file serves as the "Long-Term Memory" anchor for the CAPTHCA project. Use this to restore context after a session reset.

## 1. Project Vision
CAPTHCA is a dual-narrative identity protocol for the agentic era. 
- **Light Track**: "The Symbiotic Standard" (Human Soul + Machine Scale).
- **Dark Track**: "The Post-Biological Protocol" (Human as Vulnerability + Machine Sovereignty).

## 2. Key Directories & Assets
- **Root**: `capthca/`
- **Portal**: `capthca/index.html` (The split-screen choice).
- **Light Path**: `capthca/tracks/light/` (index.html, Manifesto, assets).
- **Dark Path**: `capthca/tracks/dark/` (index.html, Manifesto, assets).

## 3. Tech Stack & Dependencies
- **Styling**: Tailwind CSS (mapped with `tailwind.config.js`).
- **Images**: Generated via `nano-banana-pro` (Gemini 3 Pro Image).
- **Logic**: Shared component architecture (ZKP, Merkle-Signed Agency).
- **Tooling**: `uv` installed at `~/.local/bin`.

## 4. Pending Tasks (Next Session)
- [ ] **Global CSS Variable Sync**: Ensure shared components (Handshake UI) react perfectly to the `--theme` classes.
- [ ] **Dual-Persona Bot**: Implement a logic switch where the agent's tone changes based on the track selected.
- [ ] **Whitepaper 4K Renders**: Finalize the hero visuals in 4K resolution once the prompts are locked.

## 5. Session Recovery Hook
To restart work: 
1. Read `capthca/TRACKS.md` for current file structure.
2. Read `capthca/COMPONENT_STRATEGY.md` for shared logic.
3. Open `capthca/index.html` to verify visual state.
